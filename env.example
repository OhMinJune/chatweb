# 데이터베이스 연결 URL (Supabase에서 제공)
DATABASE_URL=postgresql://username:password@host:port/database

# 환경 설정
NODE_ENV=production

# 세션 시크릿 키
SESSION_SECRET=your-super-secret-session-key-here

# 포트 (Vercel에서 자동 설정)
PORT=3000
